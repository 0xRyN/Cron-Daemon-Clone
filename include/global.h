#ifndef GLOBAL_H
#define GLOBAL_H

extern char abs_path[256];  // /tmp/rayan/saturnd/pipes
extern char req_fifo[256];  // /tmp/rayan/saturnd/pipes/saturnd-request-pipe
extern char res_fifo[256];  // /tmp/rayan/saturnd/pipes/saturnd-response-pipe

#endif